<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Note Tracker App</title>
</head>

<body>
  <%- include('./partials/header'); %>
  <div class="container mt-4">
    <div class="text-center">
      <h2>Update Note</h2>
    </div>
    <br>
    <div class="text-center">
      <% if (note) { %>
        <form action="/editNote/<%= note.noteId %>" method="POST">
          <label for="title">Note Title:</label><br>
          <input type="text" id="title" name="title" value="<%= note.title %>" required><br><br>

          <label for="description">Description:</label><br>
          <textarea id="description" name="description" rows="5" cols="40" required><%= note.description %></textarea><br><br>
          <label for="category">Category:</label><br>
          <select name="categoryId" id="categoryId">
            <% for(let i=0; i < categories.length; i++) { %>
              <tr>
                <option value="<%= categories[i].categoryId %>"><%= categories[i].label %></option>
            <% } %>
          </select><br><br>
          <button type="submit" class="btn btn-primary">Update Note</button>
        </form>
      <% } else { %>
        <p>No note found.</p>
      <% } %>
      <br>
      <a href="/" class="btn btn-secondary mt-3">Back</a>
    </div>
  </div>
</body>

</html>
