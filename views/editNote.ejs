<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Note Tracker App</title>
</head>

<body>
  <%- include('./partials/header'); %>
  <div class="container mt-4">
    <div class="text-center">
      <h2>Update Note</h2>
    </div>
    <br>
    <div>
      <% if (note) { %>
        <form action="/editNote/<%= note.noteId %>" method="POST">
          <label for="title">Note Title:</label><br>
          <input type="text" class="form-control" id="title" name="title" value="<%= note.title %>" required><br><br>

          <label class="mb-2" for="description">Description:</label><br>
          <textarea class="form-control"  id="description" name="description" rows="4" cols="50" required><%= note.description %></textarea><br><br>
          <label class="mb-2" for="category">Category:</label><br>
          <select class="form-select w-25" name="categoryId" id="categoryId">
            <% for(let i=0; i < categories.length; i++) { %>
                <option value="<%= categories[i].categoryId %>"><%= categories[i].label %></option>
            <% } %>
          </select><br><br>
          <div class="d-flex">
            <button type="submit" class="btn btn-primary mr-3">Update Note</button>
            <div class="mx-3"></div>
            <a href="/" class="btn btn-secondary">Back</a>
          </div>
        </form>
      <% } else { %>
        <p>No note found.</p>
      <% } %>
      <br>
      
    </div>
  </div>
</body>

</html>
