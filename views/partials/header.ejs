<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Note App</a>
    
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if (isLoggedIn) { %>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/create">Add a new Note</a>
        </li>
      <%} %>
    </ul>
    <% if (isLoggedIn) { %>
      <div><a class="btn btn-danger" href="/logout" role="button">Logout</a></div>
    <%} %>
    <% if (!isLoggedIn) { %>
      <div><a class="btn btn-light" href="/login" role="button">Login</a></div>
      <div class="px-1"></div>
      <div><a class="btn btn-success" href="/register" role="button">Register</a></div>
    <%} %>
  </div>
</nav>